@model IEnumerable<NCKH_HANGOSELL.Models.User>

@{
    ViewData["Title"] = "List User Grid";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<div class="page-wrapper">
    <div class="content container-fluid">
        <div class="page-header">
            <div class="row">
                <div class="col-sm-12">
                    <div class="page-sub-header">
                        <h3 class="page-title">List Users</h3>
                        <ul class="breadcrumb">
                            <li class="breadcrumb-item"><a href="">Grid</a></li>
                            <li class="breadcrumb-item active">List Users</li>
                        </ul>
                    </div>
                </div>
            </div>
            <div class="row">
                <div class="col-sm-12">
                    <div class="card card-table comman-shadow">
                        <div class="card-body pb-0">
                            <div class="page-header">
                                <div class="row align-items-center">
                                    <div class="col-auto text-end float-end ms-auto download-grp">
                                        <a href="{{ route('list/users') }}" class="btn btn-outline-gray me-2 ">
                                            <i class="fa fa-list" aria-hidden="true"></i>
                                        </a>
                                        <a href="{{ route('user/grid') }}" class="btn btn-outline-gray me-2 active">
                                            <i class="fa fa-th" aria-hidden="true"></i>
                                        </a>
                                        <a href="" class="btn btn-outline-gray me-2 "><i class="fas fa-plus"></i></a>

                                    </div>
                                </div>
                            </div>

                            <div class="row pb-5 mb-4">
                                <div class="col-lg-3 col-md-6 mb-4 mb-lg-0">
                                    <!-- Card-->
                                    <div class="card shadow-lg border-0 rounded">
                                        <div class="card-body p-0">
                                            <a href="{{ url('user/profile/'.$list->id) }}">
                                                <img class="img-fluid card-img-top" alt="users Info" src="{{ url('/images/'.$list->avatar) }}" width="100" height="100">

                                                <div class="p-4">
                                                    <h5 class="mb-0 text-center"><a href="{{ url('user/profile/'.$list->id) }}">{{ $list->name }}</a></h5>
                                                    <p class="small text-muted text-center">{{ $list->role_name }}</p>

                                                </div>
                                        </div>
                                    </div>
                                </div>

                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

